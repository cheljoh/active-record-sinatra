source 'https://rubygems.org'

gem 'sinatra', require: 'sinatra/base'
gem 'sqlite3'
gem 'activerecord'
gem 'sinatra-activerecord' #gives us rake commands like rake db create migration, can run rake db migrate
gem 'pry'

group :development, :test do
  gem 'shotgun'
  gem 'minitest'
  gem 'tux' #pry-like session
  gem 'capybara'
  gem 'rack-test'
  gem 'database_cleaner'
end
